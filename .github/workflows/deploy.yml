name: Deploy Microservices to AWS EC2 (SSH)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  APP_DIR: /home/ec2-user/microservices-app

jobs:
  deploy:
    name: Deploy to EC2 via SSH
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up SSH for EC2
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

    - name: Deploy Application with SSH Authentication
      run: |
        ssh -i ~/.ssh/deploy_key ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << 'EOF'
          set -e
          
          echo "📁 Setting up deployment directory..."
          mkdir -p ${{ env.APP_DIR }}
          cd ${{ env.APP_DIR }}
          
          # Set up SSH key for GitHub access on EC2
          mkdir -p ~/.ssh
          echo "${{ secrets.REPO_SSH_KEY }}" > ~/.ssh/github_deploy_key
          chmod 600 ~/.ssh/github_deploy_key
          
          # Add GitHub to known hosts
          ssh-keyscan -H github.com >> ~/.ssh/known_hosts 2>/dev/null || true
          
          # Create SSH config for GitHub
          cat > ~/.ssh/config << 'SSH_CONFIG'
        Host github.com
          HostName github.com
          User git
          IdentityFile ~/.ssh/github_deploy_key
          StrictHostKeyChecking no
        SSH_CONFIG
          chmod 600 ~/.ssh/config
          
          # Test SSH connection to GitHub
          echo "🔗 Testing GitHub SSH connection..."
          ssh -T git@github.com || true
          
          # Clone or update repository
          if [ ! -d ".git" ]; then
            echo "🆕 First deployment - cloning repository via SSH..."
            rm -rf * .[^.]* 2>/dev/null || true
            git clone git@github.com:${{ github.repository }}.git .
          else
            echo "🔄 Updating existing repository via SSH..."
            # Update remote URL to use SSH
            git remote set-url origin git@github.com:${{ github.repository }}.git
            
            # Stash any local changes
            git stash push -m "Auto-stash before deployment" 2>/dev/null || true
            
            # Fetch and reset
            git fetch origin main
            git reset --hard origin/main
            git clean -fd
            git pull origin main
          fi
          
          # Verify we're in correct directory and files exist
          echo "📋 Current directory: $(pwd)"
          echo "📂 Directory contents:"
          ls -la
          
          # Verify docker-compose.yml exists
          if [ ! -f docker-compose.yml ]; then
            echo "❌ CRITICAL ERROR: docker-compose.yml not found!"
            echo "Available files:"
            find . -name "*.yml" -o -name "*.yaml" -o -name "docker-compose*" 2>/dev/null
            exit 1
          fi
          
          echo "✅ Found docker-compose.yml"
          
          echo "🛑 Stopping existing services..."
          sudo docker-compose down || true
          
          echo "🧹 Cleaning Docker resources..."
          sudo docker system prune -f || true
          
          echo "🔨 Building and starting services..."
          sudo docker-compose up --build -d
          
          echo "⏰ Waiting for services to initialize..."
          sleep 45
          
          echo "📊 Container status:"
          sudo docker-compose ps
          
        EOF

    - name: Health Check All Services (Improved)
      run: |
        ssh -i ~/.ssh/deploy_key ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << 'EOF'
          set -e
          echo "🔍 Navigating to deployment directory..."
          cd ${{ env.APP_DIR }} || { echo "❌ Cannot access deployment directory"; exit 1; }
          
          echo "📂 Current directory: $(pwd)"
          echo "📋 Checking for docker-compose.yml..."
          if [ ! -f docker-compose.yml ]; then
            echo "❌ CRITICAL: docker-compose.yml not found in $(pwd)"
            echo "Directory contents:"
            ls -la
            exit 1
          fi
          echo "✅ docker-compose.yml found"

          echo "=== Comprehensive Health Check ==="
          echo "📊 Overall container status:"
          sudo docker-compose ps || { echo "❌ docker-compose ps failed"; exit 1; }

          declare -A services=(
            ["user-service"]=3001
            ["post-service"]=3002
            ["comment-service"]=3003
            ["chat-service"]=3004
            ["media-service"]=3005
            ["notification-service"]=3006
            ["analytics-service"]=3007
            ["search-service"]=3008
          )

          healthy_count=0
          total_count=${#services[@]}

          echo "🏥 Testing $total_count services..."

          for service in "${!services[@]}"; do
            port=${services[$service]}

            # Confirm service exists
            if ! sudo docker-compose ps --services | grep -qw "$service"; then
              echo "🟡 Service $service not found in compose file"
              continue
            fi

            # Check if container is running
            if sudo docker-compose ps "$service" | grep -q "Up"; then
              echo "🟢 $service container is running"
              
              # Optional: check port responsiveness
              if timeout 5 bash -c "echo > /dev/tcp/localhost/$port" 2>/dev/null; then
                echo "🔵 $service is responding on port $port"
                healthy_count=$((healthy_count + 1))
              else
                echo "🔴 $service is UP but port $port is not responding"
                sudo docker-compose logs --tail=10 "$service"
              fi
            else
              echo "❌ $service container is not running"
              sudo docker-compose ps "$service"
              sudo docker-compose logs --tail=10 "$service"
            fi
          done

          echo "=========================="
          echo "🏥 Health Summary: $healthy_count/$total_count services healthy"

          if [ $healthy_count -eq $total_count ]; then
            echo "🎉 All services are healthy! Deployment successful!"
          elif [ $healthy_count -gt 0 ]; then
            echo "⚠️  Partial success ($healthy_count/$total_count)"
          else
            echo "❌ Deployment failed - no services healthy"
            sudo docker-compose logs --tail=20
            exit 1
          fi
        EOF

    - name: Display Service URLs
      if: success()
      run: |
        echo "🌐 Your microservices are now live at:"
        echo ""
        echo "   🚀 User Service:         http://${{ secrets.EC2_HOST }}:3001/health"
        echo "   📝 Post Service:         http://${{ secrets.EC2_HOST }}:3002/health"
        echo "   💬 Comment Service:      http://${{ secrets.EC2_HOST }}:3003/health"
        echo "   💬 Chat Service:         http://${{ secrets.EC2_HOST }}:3004/health"
        echo "   📁 Media Service:        http://${{ secrets.EC2_HOST }}:3005/health"
        echo "   🔔 Notification Service: http://${{ secrets.EC2_HOST }}:3006/health"
        echo "   📊 Analytics Service:    http://${{ secrets.EC2_HOST }}:3007/health"
        echo "   🔍 Search Service:       http://${{ secrets.EC2_HOST }}:3008/health"

    - name: Cleanup SSH Keys
      if: always()
      run: |
        rm -f ~/.ssh/deploy_key

    - name: Deployment Failure Help
      if: failure()
      run: |
        echo "❌ Deployment failed!"
        echo ""
        echo "🔧 Manual troubleshooting on EC2:"
        echo "1. SSH: ssh -i your-key.pem ec2-user@${{ secrets.EC2_HOST }}"
        echo "2. Check directory: cd ${{ env.APP_DIR }} && ls -la"
        echo "3. Check containers: sudo docker-compose ps"
        echo "4. Check logs: sudo docker-compose logs [service-name]"
        echo "5. Restart services: sudo docker-compose down && sudo docker-compose up -d"
